import { reelsPageMetadata, ReelsPageView } from '../../_views/reels-page'

export const metadata = reelsPageMetadata('zh')

export default function ChineseReelsPage() {
  return <ReelsPageView locale="zh" />
}
