import { reelsPageMetadata, ReelsPageView } from '../../../_views/reels-page'

export const metadata = reelsPageMetadata('en')

export default function EnglishReelsPage() {
  return <ReelsPageView locale="en" />
}
